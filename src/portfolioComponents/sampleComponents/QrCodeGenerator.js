//import { useState, useRef } from "react";
//import * as htmlToImage from "html-to-image";

const styles = {
  mainContainer: {
    display: "flex",
    width: "100%",
    height: "25rem",
    alignItems: "center",
    backgroundColor: "#77CDFF",
  },
};

const QrCodeGenerator = () => {
  return <div style={styles.mainContainer}></div>;
};

export default QrCodeGenerator;
